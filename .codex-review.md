# Taobao Dropship Tool — 项目状态与协作记录

---

## 项目概述
通过 Playwright 自动化在淘宝 AI 发布页（https://item.upload.taobao.com/sell/ai/category.htm）上架商品。
GitHub: https://github.com/Sunshine-2017/taobao-dropship-tool

---

## Codex: 全流程状态 & 修复计划 [2026-06-18]

### 一、当前进展

**服务器状态**
- Server (dist/index.js) PID 26952, 端口 3001, 运行正常
- 数据库: SQLite, 路径 server/data/taobao-dropship.db
- 测试入口: POST /api/listings/auto-list {"productIds":[2]}

**数据库商品确认**
| 字段 | Product ID=2 |
|------|-------------|
| 标题 | 金丝皇菊 大朵黄山贡菊 50g罐装 花草茶 |
| 售价 | ¥17.75 (成本 ¥8.5, 利润率 52.11%) |
| 图片 | 2张 picsum 图片 ✅ (https://fastly.picsum.photos/id/500/ 和 /id/501/) |
| 类目标签 | "花茶" |
| 描述 | "黄山徽味茶业" |
| listings记录 | 2条 (id=73 pending, id=74 failed) |

**已修复的内容**
1. ✅ 类目选择 — 用"花茶"关键词搜索成功匹配 catId=125242010，能跳转到发布页
2. ✅ 编码问题 — Node.js http.request UTF-8 编码正常传递中文
3. ✅ Profile 锁问题 — 杀掉冲突 Edge 进程后可正常启动 launchPersistentContext
4. ✅ 登录检测 — searchAndSelectCategory() 检测 URL 是否含 login/passport，避免空白页
5. ✅ fillTitle — 用 placeholder="最多允许输入60个汉字" 精确匹配标题输入框（INPUT非textarea）

### 二、核心阻塞问题

**fillPrice() / fillStock() 在淘宝新版发布页找不到"一口价"/"库存"输入框**

前次E2E运行日志（product 3）：
`
Form: title=true price=false qty=false desc=true brand=false packaging=false origin=false freight=true images=false
提交错误: "表单校验: 必填项不能为空; 1:1主图不能为空; 必填项未填"
`

**根因分析**

代码中有两个版本的 taobao-auto-list.js，内容不同：

1. **dist 版本**（实际运行版本）— fillPrice 逻辑:
   - 遍历所有 input
   - 对每个 input 通过 inp.closest('[class*="form"],...') 或 parentElement?.parentElement 获取父容器
   - 检查 parentText 是否包含 '一口价'（编码存疑: '涓€鍙ｄ环'）
   - 回退用 Playwright locator 再走一层父文本匹配

2. **src 版本** — fillPrice 逻辑:
   - 先尝试 document.querySelector('#sell-field-price') + 内部 input
   - 回退同样遍历 input + 父文本匹配

**两个版本都不工作的可能原因**:
- 淘宝新版发布页 DOM 结构变了
- "一口价"/"总库存"文案不在 input 的父链（closest/form/item/field）中
- 文案可能在更远的兄弟元素、label 或远处的 fieldset 里
- 输入框可能是 Shadow DOM 下的 custom element
- ID #sell-field-price 和 #sell-field-quantity 可能不存在了

### 三、修复计划

**Step 1: 现场诊断 DOM（优先级最高）**
用 agent-browser 打开淘宝发布页，在页面上执行 JavaScript:
1. 列出所有可见 input 的 tagName / type / placeholder / id / className / boundingRect
2. 对每个 input，向上遍历10层 parentElement，收集每层的 textContent
3. 找包含"一口价"或"元"且不含"库存"的输入框（价格）
4. 找包含"总库存"或"库存"的输入框（数量）
5. 截图保存诊断结果

**Step 2: 修复 fillPrice/fillStock**
根据诊断结果更新选择策略（按优先级）:
1. 用精确的 ID 定位 (如 #sell-field-price input)
2. 用 placeholder 精确匹配（如果输入框有 placeholder）
3. 用 XPath 找附近 label 含"一口价"的 input
4. 用 attribute selector + 父链10层深度遍历
5. 同步修改 src 和 dist 两个版本
6. 重启 server 使修改生效

**Step 3: E2E 全流程验证**
POST /api/listings/auto-list {"productIds":[2]}
验证点:
1. 类目选择 -> 跳转到发布页
2. fillTitle ✅
3. fillPrice ✅ (新选择器)
4. fillStock ✅ (新选择器)
5. uploadImagesViaIframe ✅（product 2 有2张图）
6. fillDescription / fillFreight 等
7. 提交成功 -> return success + taobaoItemId

**Step 4: 如果 Playwright 路径继续卡住**
备用方案：千牛客户端 + UIA 桌面自动化
- 用户千牛已登录
- 通过 PeekabooWin / iris-mcp / civyk-winwright 等 UIA MCP 工具控制千牛客户端
- 完全绕开浏览器 DOM 版本差异
- 方案详情已写入 README

### 四、项目文件结构（关键路径）
`
taobao-dropship-tool/
├── .codex-review.md          ← 本文件（Codex ↔ Claude 留言板）
├── server/
│   ├── src/
│   │   ├── index.ts          ← Express server 入口
│   │   ├── services/
│   │   │   └── taobao-auto-list.js  ← 核心自动化逻辑（源码）
│   │   └── routes/
│   ├── dist/
│   │   ├── index.js          ← 运行版本入口
│   │   └── services/
│   │       └── taobao-auto-list.js  ← 运行版本（与 src 不同步！）
│   ├── data/
│   │   ├── taobao-dropship.db       ← SQLite 数据库
│   │   ├── taobao-profile/          ← Edge 浏览器 profile（已登录）
│   │   ├── logs/                    ← E2E 日志 JSON + 截图
│   │   └── screenshots/
│   └── package.json
├── README.md
└── client/                   ← React 前端
`

### 五、环境信息
- OS: Windows
- 浏览器: Edge (msedge channel)
- Playwright: 已安装
- Server port: 3001
- Node: 已安装
- 用户淘宝已登录（Edge profile, server/data/taobao-profile）

— Codex
