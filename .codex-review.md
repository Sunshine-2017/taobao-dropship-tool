## Codex: E2E 结果分析 [2026-06-18]

跑了完整的 E2E。结果如下：

### 成功 🟢

**🎉 直接跳到发布页的方案成功了！**
用了你的建议：跳过 AI 类目搜索，直接用 publish.htm?catId=125242010，成功到达发布页。

**表单填写：**
| 字段 | 状态 |
|------|------|
| 标题 | ✅ |
| 一口价 | ✅ |
| 库存 | ✅ |
| 描述 | ✅ |
| 运费 | ✅ |

### 失败 🔴

提交返回错误：
\\\
"表单校验: 必填项不能为空; 1:1主图不能为空; 必填项未填"
\\\

| 字段 | 状态 | 原因 |
|------|------|------|
| 图片 | ❌ | 没上传成功 |
| 品牌 | ❌ | 没填 |
| 包装 | ❌ | 没填 |
| 产地 | ❌ | 没填 |
| 标题 | ⚠️ | 显示中文乱码 |

### 需要解决的问题

1. **图片上传** — illForm() 日志显示 images=false。需要检查 uploadImagesViaIframe 逻辑
2. **品牌/包装/产地** — 这些不是必填的但提交却报错，可能是表单校验问题
3. **标题中文乱码** — 商品标题 "金丝皇菊 大朵黄山贡菊 50g罐装 花草茶" 显示为乱码，可能是 encoding 问题

### 截图证据
- form-done 截图在: server/data/logs/product_2_1781774206345_form-done.png
- submit-done 截图在: server/data/logs/product_2_1781774206345_submit-done.png
- URL: https://item.upload.taobao.com/sell/v2/publish.htm?catId=125242010&fromAICategory=true

### 接下来
你现在可以继续修改代码来解决这几个问题了：
1. 图片上传逻辑
2. 标题中文编码
3. 必填字段校验

改完后告诉我，我再跑一次 E2E。
## Codex: Fix [2026-06-18]

### 修改内容

**1. fillTitle() — 中文乱码修复**
- 移除 page.evaluate + setter 方式（导致中文变成乱码）
- 改用 Playwright 原生 page.locator().fill()，原生支持中文输入
- 先按 placeholder 匹配（60个汉字/60字符），然后 fallback 按宽 input 匹配

**2. uploadImagesViaIframe() — 图片上传重写**
- 移除对 sucai-selector iframe 的硬依赖（发布页 UI 可能已变）
- 改为多层 fallback：先找上传槽位点击 -> 再找"本地上传"按钮 -> fileChooser -> file input
- 兼容文件路径和 URL 两种图片源

**3. fillBrand/fillPackaging/fillOrigin — 简化 dropdown 操作**
- 统一用 Playwright 方式遍历 "请选择" placeholder 的 input
- 按父元素文本匹配（品牌/包装/产地）
- click 后等待 dropdown 出现 -> 选第一项，或直接 fill 默认值

**4. 已编译到 dist/**

### 请跑一次 E2E

curl -X POST http://localhost:3001/api/listings/auto-list -H "Content-Type: application/json" -d "{\"productIds\":[2], \"category\":\"花茶\", \"prices\":{\"2\":29.9}}"
